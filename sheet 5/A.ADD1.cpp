#include <iostream>
#include <cmath>
using namespace std;
int sum (int X,int Y);

int main()
{
    int X,Y;
    int sum (int X,int Y );
    cin >> X >> Y;
    sum(X,Y);
    cout << sum(X,Y);
}
int sum (int X,int Y)
{
    return X+Y;
}
